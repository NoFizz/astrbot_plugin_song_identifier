# Changelog

## [1.1.0] - 2026-08-12

### Added
- 失败自动重试：识别引擎与封面下载遇到网络错误等瞬时故障时自动重试，新增「失败重试次数」（默认 2）与「失败重试间隔（秒）」（默认 2）配置项（高级设置，位于「识别超时」之前）；认证/配置/无结果类失败不重试
- 统一代理策略：新增「HTTP 代理地址」配置项（高级设置），网易云/QQ 搜索、封面下载、识别引擎请求统一生效；留空时自动回退环境变量 `HTTP_PROXY`/`HTTPS_PROXY`，再回退直连
- README 增加使用示例插图（语音/音频文件/视频三种消息类型与文本/图片/卡片三种输出形式）

### Fixed
- LLM 识曲工具路径增加异常保护，意外错误不再直接抛出工具调用
- ffprobe 探测超时/取消时正确终止并回收子进程，不留孤儿进程
- QQ 音乐与网易云增强增加歌手校验，避免同歌名不同歌手误配歌曲 ID
- 网易云搜索增加标题校验，识别结果错误时不再生成错歌链接/封面；并修复跨语言歌手写法（如罗马音/片假名）导致的封面缺失
- 图片卡片渲染移入线程池，不再阻塞事件循环；封面下载增加网易云 Referer 头
- 讯飞 MP3 转码临时文件改写入 AstrBot 临时目录，与媒体工件生命周期一致
- Shazam 引擎回退环境变量代理，与其余网络请求策略一致
- 修复重试次数配置为 0（关闭重试）时被忽略的问题

### Changed
- 识别超时配置说明澄清：不包含识别完成后的网易云/QQ 增强搜索

## [1.0.1] - 2026-08-11

### Added
- QQ 音乐歌曲 ID（songmid）查询，增强 QQ 音乐卡片平台回退时的歌曲信息

### Changed
- 插件描述（metadata.yaml `desc` / README / 仓库描述）更新为「支持视频文件、音频文件、语音消息听歌识曲/音乐识别」

### Fixed
- 识别结果改为请求级对象，避免并发识别时结果相互串扰
- 增加并发闸门与媒体大小上限，并支持超时/取消时正确回收 ffmpeg 子进程
- 识别完成后清理 AstrBot 临时目录中的源文件，同时确保不触碰用户文件
- 记录 aiofiles 版本冲突实为上游元数据问题（不影响实际兼容性）
